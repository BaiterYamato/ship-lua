# Handoff — MOD-001 / MOD-002 / CORE-002

## Estado

review

## Resultado

- Manifesto representado por `Manifest` e documentado por JSON Schema válido.
- Parser TOML lê campos obrigatórios e opcionais e retorna erros estruturados.
- `ModHost` mantém um `LuaRuntime` isolado por ID, rejeita duplicatas e faz rollback em falha.
- Carregamento por diretório bloqueia caminhos absolutos, `..` e symlinks no entrypoint.
- CMake integra `toml++` e compila recursivamente os componentes do núcleo.

## Commits

- `61c7675` — docs(governance): allow authorized commits
- `be9d199` — feat(modloader): add manifest host

## Arquivos alterados

- `AGENTS.md`
- `CMakeLists.txt`
- `schema/manifest.schema.json`
- `include/shiplua/manifest/Manifest.h`
- `include/shiplua/manifest/ManifestParser.h`
- `src/manifest/ManifestParser.cpp`
- `include/shiplua/host/ModHost.h`
- `src/host/ModHost.cpp`
- `tests/CMakeLists.txt`
- `tests/unit/ManifestHostTests.cpp`
- `coordination/claims/MOD-001.md`
- `coordination/STATUS.md`

## Validação executada

```powershell
cmake -S . -B build -G Ninja -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++
cmake --build build
ctest --test-dir build --output-on-failure
Get-Content schema/manifest.schema.json | ConvertFrom-Json
git diff --check
```

## Resultado da validação

- Windows 11, MinGW g++ 15.2, Ninja: build concluído.
- 2/2 testes CTest passaram (`runtime_tests` e `manifest_host_tests`).
- JSON Schema parseado com sucesso.
- `git diff --check` sem erros.

## Pendências

- Build/test ainda não executados em Linux e macOS.
- Validação SemVer e resolução de dependências pertencem a MOD-004..MOD-007.
- Fixtures extensivas válidas/inválidas permanecem em TEST-002.

## Riscos

- `FetchContent` ainda exige rede na primeira configuração.
- O parser ignora chaves desconhecidas por compatibilidade futura.

## Próxima ação recomendada

Revisar e integrar esta branch; depois iniciar TEST-002/MOD-003.

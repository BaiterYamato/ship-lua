> English translation of [coordination/handoffs/API-001.md](API-001.md). The Portuguese document remains the canonical project record.

# Handoff — API-001 / API-002 / API-003

## State

review

## Result

- Canonical schemas define API `0.1.0`, events and capabilities.
- Public types use handles/snapshots and do not expose pointers or internal layouts.
- Events record type, phase, cancellation, support, capability and payload.
- Capabilities distinguish active contract from just planned resources.
- Validator crosses references, hosts, versions, errors and security rules.
- Negative tests cover orphan, planned, unknown type and pointer capabilities.

## Commits

- `e1e6e6d` — feat(api): defines canonical schemas

## Changed files

- `schema/api.yml`
- `schema/events.yml`
- `schema/capabilities.yml`
- `tools/validate_api_schemas.py`
- `tests/schema/test_validate_api_schemas.py`
- `tests/CMakeLists.txt`
- `docs/api/schema-contract.md`
- `.gitignore`
- `coordination/claims/API-001.md`
- `coordination/STATUS.md`

## Validation performed

```powershell
python tools/validate_api_schemas.py
python tests/schema/test_validate_api_schemas.py
ctest --test-dir build-verify --output-on-failure
git diff --check
rg --files | rg "\.(z64|n64|v64|o2r)$"
```

## Validation result

- Canonical validator: API `0.1.0` valid.
- 7 Python tests passed.
- 12/12 CTest tests passed.
- Interface UTF-8 validated by byte inspection in subprocess.
- `git diff --check` and protected asset scanner no finds.

## Fault found and feedback

- The first manual run used Windows code page and corrupted accents.
- Regression rule: Python CLIs must emit explicit UTF-8 on stdout/stderr.
- `test_cli_emits_utf8_on_windows` covers the expected bytes; there is no `SPEC.md` for §B/§V.

## Pending

- Approval of the RFC and schemas by the integrator.
- EVENT-001 must consume names/types from these schemas without duplicating them.
- CODEGEN-001/002/003 should generate artifacts exclusively from this source.

## Risks

- `.yml` files use JSON syntax, valid in YAML 1.2, to avoid PyYAML dependency.
- Specific capabilities remain `planned` and cannot be referenced by the active contract.
- This branch depends on PR #6 and should remain stacked until integration.

## Recommended next action

Review schemas and start EVENT-001 with order and unsubscribe tests.

# Handoff — OOT-002

## Estado

review

## Resultado

- O Shipwright inicializa uma instância única de `ShipLua::ModHost` após criar o `GameInteractor`.
- Logs `debug`, `info`, `warn` e `error` do ShipLua são encaminhados ao `spdlog` do host.
- O bootstrap é idempotente e expõe o host para as próximas tarefas sem carregar mods.
- `shiplua`, `lua_static` e `miniz` usam o mesmo CRT estático do host no MSVC.
- O PR BaiterYamato/Shipwright-HyliaFoundry#6 foi aberto sobre OOT-001.

## Commits

- `fb3bead9a` — `feat(oot): bootstrap ShipLua runtime`

## Arquivos alterados

- `CMakeLists.txt`
- `soh/soh/OTRGlobals.cpp`
- `soh/soh/ShipLuaBootstrap.cpp`
- `soh/soh/ShipLuaBootstrap.h`

## Validação executada

Windows 11, Visual Studio 2022 Community, MSVC 19.44, Release x64:

```powershell
cmake -S . -B build-msvc-shiplua -G "Visual Studio 17 2022" -A x64 -DSUPPRESS_WARNINGS=0
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
git diff --check
```

## Resultado da validação

- Configure MSVC passou.
- O primeiro link funcional revelou incompatibilidade `/MD` versus `/MT`; o contrato foi corrigido nos três targets internos.
- O build completo corrigido passou e gerou `x64/Release/soh.exe` com 40.457.728 bytes.
- O rebuild incremental também passou.
- Nenhum ROM, OTR/O2R, save ou ativo protegido foi versionado.

## Pendências

- O executável não foi iniciado porque o teste local exigiria ativos legítimos não incluídos no repositório.
- Linux e macOS não foram executados localmente.
- O shutdown explícito pertence a OOT-003; versão/commit pertencem a OOT-004.

## Riscos

- Até OOT-003, a instância permanece viva até a finalização estática do processo.
- Warnings preexistentes do host e das dependências continuam presentes no MSVC.

## Próxima ação recomendada

Executar OOT-003 em branch própria para destruir o host antes do encerramento dos serviços dependentes.

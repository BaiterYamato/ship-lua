# Handoff — MM-005

## Estado

review

## Resultado

- O 2Ship cria e usa a pasta gravável `mods` do aplicativo.
- Pacotes usam o cache isolado `mods/.shiplua-cache`.
- A carga passa pelo pipeline comum e publica `game.ready` após o bootstrap.
- Rejeições e falhas de callback são registradas sem derrubar o host.

## Commits

- `7c84358` — feat(mm): load Lua mods from app directory

## Arquivos alterados

- `extern/ship-lua`
- `mm/2s2h/ShipLuaBootstrap.cpp`

## Validação executada

```powershell
cmake --build build-msvc-shiplua --config Release --target 2ship -- /m:1
cmake --build build-msvc-shiplua --config Release --target 2ship -- /m:1
```

## Resultado da validação

- Windows 11, Visual Studio 2022, toolset v143, Release.
- Build completo e repetição incremental concluídos; `2ship.exe` gerado.
- `git diff --check` passou.

## Pendências

- Execução com ROM/ativos legítimos não realizada.
- Linux e macOS não validados.

## Riscos

- O host mantém warnings preexistentes de compilação, sem erro novo do ShipLua.

## Próxima ação recomendada

Integrar MOD-008 e MM-004 antes do PR #5.

# Handoff — OOT-004

## Estado

review

## Resultado

- O bootstrap injeta `gameId = "oot"` no `LuaApiHostContext`.
- `ship.game.host_version()` recebe o SemVer `9.1.2`, derivado das variáveis geradas do host.
- O commit gerado pelo CMake é detectado separadamente e registrado no log de bootstrap.
- O PR BaiterYamato/Shipwright-HyliaFoundry#8 foi aberto sobre OOT-003.

## Commits

- `0afc0eb60` — `feat(oot): provide ShipLua host identity`

## Arquivos alterados

- `soh/soh/ShipLuaBootstrap.cpp`

## Validação executada

Windows 11, Visual Studio 2022 Community, MSVC 19.44, Release x64:

```powershell
cmake -S . -B build-msvc-shiplua -G "Visual Studio 17 2022" -A x64 -DSUPPRESS_WARNINGS=0
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
git diff --check HEAD^ HEAD
```

## Resultado da validação

- A reconfiguração gerou versão `9.1.2` e commit `0afc0eb` no host.
- O build passou e gerou `x64/Release/soh.exe` com 40.461.312 bytes.
- O rebuild incremental também passou sem recompilações.
- O commit altera apenas o bootstrap reivindicado.

## Pendências

- O executável não foi iniciado porque o teste local exigiria ativos legítimos não incluídos no repositório.
- Linux e macOS não foram executados localmente.
- A descoberta do diretório de mods pertence a OOT-005.

## Riscos

- O commit está disponível ao bootstrap/log, mas não foi adicionado à API Lua pública.
- Warnings preexistentes do host e das dependências continuam presentes no MSVC.

## Próxima ação recomendada

Executar OOT-005 em branch própria para localizar o diretório de mods sem acesso irrestrito ao filesystem.

> English translation of [coordination/handoffs/MOD-008.md](MOD-008.md). The Portuguese document remains the canonical project record.

# Handoff — MOD-008

## State

done

## Result

- The mod root accepts `.shipmod` directories and packages.
- Game, API and host compatibility is validated before execution.
- Dependencies are ordered in the nucleus; faults are isolated by mod.
- Rejected extracts are removed from the cache.

## Commits

- `438e226` — feat(mods): load compatible mods from root

## Changed files

- `include/shiplua/host/ModHost.h`
- `src/host/ModHost.cpp`
- `tests/unit/ModRootLoaderTests.cpp`
- `tests/CMakeLists.txt`
- `PLAN.md`

## Validation performed

```powershell
cmake --build build -j 2
ctest --test-dir build --output-on-failure
```

## Validation result

- Windows 11, CMake + MinGW: build completed.
- 21 out of 21 tests passed.

## Pending

- Validate Linux and macOS.
- Execute a real package on both hosts with legitimate assets.

## Risks

- The persistent cache does not yet have an expiration policy per package version.

## Recommended next action

MOD-008 is already integrated into `main` by commit `3657810`; integrate OOT-005 and MM-005 after respective identity tasks.

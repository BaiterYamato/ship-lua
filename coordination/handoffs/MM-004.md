# Handoff — MM-004

## Estado

review

## Resultado

- O bootstrap injeta `gameId = "mm"` no `LuaApiHostContext`.
- `ship.game.host_version()` recebe o SemVer `4.0.2`, derivado das variáveis geradas do host.
- O commit gerado pelo CMake é detectado separadamente e registrado no log de bootstrap.
- O PR BaiterYamato/2ship2harkinian#4 foi aberto sobre MM-003.

## Commits

- `084cdad` — `feat(mm): provide ShipLua host identity`

## Arquivos alterados

- `mm/2s2h/ShipLuaBootstrap.cpp`

## Validação executada

Windows 11, Visual Studio 2022 Community, MSVC 19.44, Release x64:

```powershell
cmake -S . -B build-msvc-shiplua -G "Visual Studio 17 2022" -A x64
cmake --build build-msvc-shiplua --config Release --target 2ship -- /m:1
cmake --build build-msvc-shiplua --config Release --target 2ship -- /m:1
git diff --check HEAD^ HEAD
```

## Resultado da validação

- A reconfiguração gerou versão `4.0.2` e commit `084cdad` no host.
- O build passou e gerou `x64/Release/2ship.exe` com 22.895.104 bytes.
- O rebuild incremental também passou sem recompilações.
- O commit altera apenas o bootstrap reivindicado.

## Pendências

- O executável não foi iniciado porque o teste local exigiria ativos legítimos não incluídos no repositório.
- Linux e macOS não foram executados localmente.
- A descoberta do diretório de mods pertence a MM-005.

## Riscos

- O commit está disponível ao bootstrap/log, mas não foi adicionado à API Lua pública.
- Warnings preexistentes do host e das dependências continuam presentes no MSVC.

## Próxima ação recomendada

Executar MM-005 em branch própria para localizar o diretório de mods sem acesso irrestrito ao filesystem.

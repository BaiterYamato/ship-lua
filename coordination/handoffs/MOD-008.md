# Handoff — MOD-008

## Estado

done

## Resultado

- A raiz de mods aceita diretórios e pacotes `.shipmod`.
- Compatibilidade de jogo, API e host é validada antes da execução.
- Dependências são ordenadas no núcleo; falhas ficam isoladas por mod.
- Extrações rejeitadas são removidas do cache.

## Commits

- `438e226` — feat(mods): load compatible mods from root

## Arquivos alterados

- `include/shiplua/host/ModHost.h`
- `src/host/ModHost.cpp`
- `tests/unit/ModRootLoaderTests.cpp`
- `tests/CMakeLists.txt`
- `PLAN.md`

## Validação executada

```powershell
cmake --build build -j 2
ctest --test-dir build --output-on-failure
```

## Resultado da validação

- Windows 11, CMake + MinGW: build concluído.
- 21 de 21 testes passaram.

## Pendências

- Validar Linux e macOS.
- Executar um pacote real nos dois hosts com ativos legítimos.

## Riscos

- O cache persistente ainda não possui política de expiração por versão do pacote.

## Próxima ação recomendada

MOD-008 já está integrado em `main` pelo commit `3657810`; integrar OOT-005 e MM-005 após as respectivas tarefas de identidade.

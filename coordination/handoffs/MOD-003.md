# Handoff — MOD-003 / TEST-002

## Estado

review

## Resultado

- Discovery examina somente filhos diretos do diretório de mods.
- Diretórios exigem `manifest.toml` regular e pacotes exigem extensão `.shipmod` mais assinatura ZIP.
- Symlinks não são seguidos e a saída é ordenada deterministicamente por caminho.
- Fixtures cobrem manifestos mínimo/completo, campo obrigatório ausente e sintaxe inválida.
- Pastas e arquivos que não representam mods são ignorados.
- Pacotes são extraídos em staging e publicados por rename somente após validação completa.
- Limites cobrem entradas, bytes por arquivo, bytes totais e razão de compressão.
- Paths absolutos, traversal, symlinks, colisões por caixa e colisões arquivo/diretório são rejeitados.
- `ModHost` carrega `.shipmod` e remove a extração no unload ou em qualquer falha.

## Commits

- `677f633` — feat(modloader): discover mod sources
- `693cfa2` — security(modloader): extract packages safely

## Arquivos alterados

- `include/shiplua/manifest/ModDiscovery.h`
- `src/manifest/ModDiscovery.cpp`
- `include/shiplua/manifest/PackageExtractor.h`
- `src/manifest/PackageExtractor.cpp`
- `include/shiplua/host/ModHost.h`
- `src/host/ModHost.cpp`
- `CMakeLists.txt`
- `tests/fixtures/manifest/*.toml`
- `tests/unit/ManifestDiscoveryTests.cpp`
- `tests/unit/PackageExtractorTests.cpp`
- `tests/CMakeLists.txt`
- `coordination/claims/MOD-003.md`
- `coordination/STATUS.md`

## Validação executada

```powershell
cmake -S . -B build-verify -G Ninja -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++
cmake --build build-verify
ctest --test-dir build-verify --output-on-failure
git diff --check
rg --files | rg "\.(z64|n64|v64|o2r)$"
```

## Resultado da validação

- Windows 11 / MinGW g++ 15.2 / Ninja: build concluído.
- Configuração limpa e build completo concluídos.
- 4/4 testes CTest passaram.
- `git diff --check` sem erros.
- Nenhum ativo protegido encontrado.

## Falha encontrada e retroalimentação

- A primeira execução falhou porque `symlink_status` reporta arquivo inexistente como erro no Windows.
- Regra de regressão: filhos sem `manifest.toml` devem ser ignorados, não abortar a varredura.
- O teste `TestDiscovery` cobre o caso; o repositório não possui `SPEC.md` para registro §B/§V.
- O primeiro teste de path absoluto falhou porque o writer recusou gerar a fixture hostil.
- A fixture agora muta headers ZIP de tamanho constante e exercita efetivamente o leitor malicioso.

## Pendências

- Executar testes em Linux e macOS.

## Riscos

- `miniz` 3.1.2 é obtido por FetchContent e exige rede na primeira configuração.
- O cache extraído é removido no unload; persistência de assets em reload será tratada na fase RELOAD.
- Esta branch depende do PR #1 e deve permanecer empilhada até sua integração.

## Próxima ação recomendada

Integrar PR #1, atualizar a base desta branch sem rebase compartilhado e iniciar MOD-004.

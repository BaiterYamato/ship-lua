# Handoff — OOT-001

## Estado

review

## Resultado

- A branch remota `lua/main` foi restaurada no commit baseline `fb3259f7`.
- `extern/ship-lua` foi adicionado como submódulo e pinado em `ad5aad6`.
- O CMake raiz inclui o núcleo compartilhado e `soh` liga contra `shiplua`.
- O modo standalone de `soh` também inclui o subdiretório quando necessário.
- O PR BaiterYamato/Shipwright-HyliaFoundry#5 foi aberto contra `lua/main`.

## Commits

- `31be7ba8a` — `build(oot): integrate ShipLua submodule`

## Arquivos alterados

- `.gitmodules`
- `CMakeLists.txt`
- `extern/ship-lua`
- `soh/CMakeLists.txt`

## Validação executada

Windows 11, Visual Studio 2022 Community, MSVC 19.44, Release x64:

```powershell
cmake -S . -B build-msvc-shiplua -G "Visual Studio 17 2022" -A x64 -DSUPPRESS_WARNINGS=0
cmake --build build-msvc-shiplua --config Release --target shiplua -- /m:1
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
git diff --check
```

## Resultado da validação

- Configure MSVC passou.
- `shiplua.lib` foi gerado.
- O build completo passou e gerou `x64/Release/soh.exe`.
- O configure Ninja/MinGW alcançou o ShipLua, mas a geração do host falhou no uso preexistente de `$<TARGET_PDB_FILE:soh>`, incompatível com esse toolchain.
- Nenhum ROM, OTR/O2R, save ou ativo protegido foi versionado.

## Pendências

- Linux e macOS não foram executados localmente.
- O bootstrap do runtime permanece fora deste PR.
- O pin `ad5aad6` depende da pilha de PRs do ShipLua em revisão.

## Riscos

- O submódulo deve ser atualizado para um commit de integração estável após o merge da pilha do ShipLua.
- Warnings preexistentes do host e das dependências continuam presentes no build MSVC.

## Próxima ação recomendada

Executar OOT-002 em branch própria após revisão do PR #5.

# Handoff — API-001 / API-002 / API-003

## Estado

review

## Resultado

- Schemas canônicos definem API `0.1.0`, eventos e capabilities.
- Tipos públicos usam handles/snapshots e não expõem ponteiros ou layouts internos.
- Eventos registram kind, fase, cancelamento, suporte, capability e payload.
- Capabilities distinguem contrato ativo de recursos apenas planejados.
- Validador cruza referências, hosts, versões, erros e regras de segurança.
- Testes negativos cobrem capability órfã, planejada, tipo desconhecido e ponteiro.

## Commits

- `e1e6e6d` — feat(api): define canonical schemas

## Arquivos alterados

- `schema/api.yml`
- `schema/events.yml`
- `schema/capabilities.yml`
- `tools/validate_api_schemas.py`
- `tests/schema/test_validate_api_schemas.py`
- `tests/CMakeLists.txt`
- `docs/api/schema-contract.md`
- `.gitignore`
- `coordination/claims/API-001.md`
- `coordination/STATUS.md`

## Validação executada

```powershell
python tools/validate_api_schemas.py
python tests/schema/test_validate_api_schemas.py
ctest --test-dir build-verify --output-on-failure
git diff --check
rg --files | rg "\.(z64|n64|v64|o2r)$"
```

## Resultado da validação

- Validador canônico: API `0.1.0` válida.
- 7 testes Python passaram.
- 12/12 testes CTest passaram.
- UTF-8 da interface validado por inspeção de bytes em subprocesso.
- `git diff --check` e scanner de ativos protegidos sem achados.

## Falha encontrada e retroalimentação

- A primeira execução manual usou code page do Windows e corrompeu acentos.
- Regra de regressão: CLIs Python devem emitir UTF-8 explícito em stdout/stderr.
- `test_cli_emits_utf8_on_windows` cobre os bytes esperados; não há `SPEC.md` para §B/§V.

## Pendências

- Aprovação da RFC e dos schemas pelo integrador.
- EVENT-001 deve consumir nomes/tipos destes schemas sem duplicá-los.
- CODEGEN-001/002/003 deve gerar artefatos exclusivamente desta fonte.

## Riscos

- Arquivos `.yml` usam sintaxe JSON, válida em YAML 1.2, para evitar dependência PyYAML.
- Capabilities específicas permanecem `planned` e não podem ser referenciadas pelo contrato ativo.
- Esta branch depende do PR #6 e deve permanecer empilhada até integração.

## Próxima ação recomendada

Revisar schemas e iniciar EVENT-001 com testes de ordem e unsubscribe.

# Handoff — OOT-005

## Estado

review

## Resultado

- O Shipwright cria e usa a pasta gravável `mods` do aplicativo.
- Pacotes usam o cache isolado `mods/.shiplua-cache`.
- A carga passa pelo pipeline comum e publica `game.ready` após o bootstrap.
- Rejeições e falhas de callback são registradas sem derrubar o host.

## Commits

- `e865ec314` — feat(oot): load Lua mods from app directory

## Arquivos alterados

- `extern/ship-lua`
- `soh/soh/ShipLuaBootstrap.cpp`

## Validação executada

```powershell
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
```

## Resultado da validação

- Windows 11, Visual Studio 2022, toolset v143, Release.
- Build completo e repetição incremental concluídos; `soh.exe` gerado.
- `git diff --check` passou.

## Pendências

- Execução com ROM/ativos legítimos não realizada.
- Linux e macOS não validados.

## Riscos

- O host mantém warnings preexistentes de compilação e link, sem erro novo do ShipLua.

## Próxima ação recomendada

Integrar MOD-008 e OOT-004 antes do PR #9.

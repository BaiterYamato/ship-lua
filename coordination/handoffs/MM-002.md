# Handoff — MM-002

## Estado

review

## Resultado

- O 2Ship inicializa uma instância única de `ShipLua::ModHost` após criar o `GameInteractor`.
- Logs `debug`, `info`, `warn` e `error` do ShipLua são encaminhados ao `spdlog` do host.
- O bootstrap é idempotente e expõe o host para as próximas tarefas sem carregar mods.
- `shiplua`, `lua_static` e `miniz` usam o mesmo CRT estático do host no MSVC.
- O PR BaiterYamato/2ship2harkinian#2 foi aberto sobre MM-001.

## Commits

- `ade132e` — `feat(mm): bootstrap ShipLua runtime`

## Arquivos alterados

- `CMakeLists.txt`
- `mm/2s2h/BenPort.cpp`
- `mm/2s2h/ShipLuaBootstrap.cpp`
- `mm/2s2h/ShipLuaBootstrap.h`

## Validação executada

Windows 11, Visual Studio 2022 Community, MSVC 19.44, Release x64:

```powershell
cmake -S . -B build-msvc-shiplua -G "Visual Studio 17 2022" -A x64
cmake --build build-msvc-shiplua --config Release --target 2ship -- /m:1
cmake --build build-msvc-shiplua --config Release --target 2ship -- /m:1
git diff --check
```

## Resultado da validação

- Configure MSVC passou.
- O primeiro link funcional revelou incompatibilidade `/MD` versus `/MT`; o contrato foi corrigido nos três targets internos.
- O build completo corrigido passou e gerou `x64/Release/2ship.exe` com 22.893.056 bytes.
- O rebuild incremental também passou.
- Nenhum ROM, OTR/O2R, save ou ativo protegido foi versionado.

## Pendências

- O executável não foi iniciado porque o teste local exigiria ativos legítimos não incluídos no repositório.
- Linux e macOS não foram executados localmente.
- O shutdown explícito pertence a MM-003; versão/commit pertencem a MM-004.

## Riscos

- Até MM-003, a instância permanece viva até a finalização estática do processo.
- Warnings preexistentes do host e das dependências continuam presentes no MSVC.

## Próxima ação recomendada

Executar MM-003 em branch própria para destruir o host antes do encerramento dos serviços dependentes.

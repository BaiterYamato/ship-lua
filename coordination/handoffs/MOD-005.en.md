> English translation of [coordination/handoffs/MOD-005.md](MOD-005.md). The Portuguese document remains the canonical project record.

# Handoff — MOD-005 / MOD-006 / MOD-007

## State

review

## Result

- Mandatory dependencies are validated by SemVer presence and range.
- Rejections only propagate to dependents; Independent mods remain loadable.
- Hints `after`/`before` present create edges; missing hints are ignored.
- Topological sorting uses ascending priority and ID as a deterministic tiebreaker.
- Duplicate IDs, invalid versions, incompatibilities and cycles are reported per mod.
- The final order does not depend on the order in which the manifests are enumerated.

## Commits

- `ed1a0c5` — feat(modloader): resolves dependency graph

## Changed files

- `include/shiplua/manifest/DependencyResolver.h`
- `src/manifest/DependencyResolver.cpp`
- `tests/unit/DependencyResolverTests.cpp`
- `tests/CMakeLists.txt`
- `coordination/claims/MOD-005.md`
- `coordination/STATUS.md`

## Validation performed

```powershell
cmake --build build-verify
ctest --test-dir build-verify --output-on-failure
git diff --check
rg --files | rg "\.(z64|n64|v64|o2r)$"
```

## Validation result

- Windows 11 / MinGW g++ 15.2 / Ninja: build completed.
- 6/6 CTest tests passed.
- `git diff --check` no errors.
- No protected assets found.

## Pending

- Connect the resolved order to a `ModHost` batch operation when the first vertical slice is assembled.
- Run tests on Linux and macOS.

## Risks

- `ModResolution::rejected` aggregates conflicts by ID; two duplicate instances share a diagnosis.
- Hints for missing mods are intentionally ignored and do not constitute dependency.
- This branch depends on PR #3 and must remain stacked until its integration.

## Recommended next action

Integrate the stack in order and proceed to TOOL-001 or API-001/RFC.

# Handoff — MOD-004

## Estado

review

## Resultado

- Parser SemVer 2.0.0 valida core, prerelease e build metadata.
- Comparação segue precedência SemVer, ignorando build metadata.
- Ranges conjuntivos suportam `>`, `>=`, `<`, `<=`, `=` e versão exata.
- Comparadores aceitam versões parciais e completam componentes ausentes com zero.
- Manifestos validam `version`, `api`, ranges de host e ranges de dependências.
- Campos obrigatórios vazios são rejeitados como ausentes.

## Commits

- `df474f7` — feat(manifest): resolve semantic versions

## Arquivos alterados

- `include/shiplua/manifest/SemVersion.h`
- `src/manifest/SemVersion.cpp`
- `src/manifest/ManifestParser.cpp`
- `tests/unit/SemVersionTests.cpp`
- `tests/unit/ManifestDiscoveryTests.cpp`
- `tests/fixtures/manifest/invalid-version.toml`
- `tests/CMakeLists.txt`
- `coordination/claims/MOD-004.md`
- `coordination/STATUS.md`

## Validação executada

```powershell
cmake --build build-verify
ctest --test-dir build-verify --output-on-failure
git diff --check
rg --files | rg "\.(z64|n64|v64|o2r)$"
```

## Resultado da validação

- Windows 11 / MinGW g++ 15.2 / Ninja: build concluído.
- 5/5 testes CTest passaram.
- `git diff --check` sem erros.
- Nenhum ativo protegido encontrado.

## Pendências

- Operador OR, caret, tilde e wildcards não fazem parte do contrato atual.
- Resolver dependências contra versões instaladas em MOD-005.
- Executar testes em Linux e macOS.

## Riscos

- Componentes core são limitados a `uint64_t`; valores maiores são rejeitados.
- Ranges prerelease usam comparação SemVer direta, sem regra adicional semelhante ao npm.
- Esta branch depende do PR #2 e deve permanecer empilhada até sua integração.

## Próxima ação recomendada

Integrar PRs #1 e #2, depois este PR; em seguida iniciar MOD-005.

# Handoff — OOT-003

## Estado

review

## Resultado

- `ShipLuaHost::Shutdown()` destrói explicitamente a instância única do `ModHost`.
- O shutdown é idempotente e ocorre em `DeinitOTR`, antes de áudio, GUI, contexto e logger.
- A destruição do `ModHost` executa `UnloadAll()`, descarregando os mods ainda ativos.
- O PR BaiterYamato/Shipwright-HyliaFoundry#7 foi aberto sobre OOT-002.

## Commits

- `783cc8f84` — `fix(oot): shut down ShipLua runtime`

## Arquivos alterados

- `soh/soh/OTRGlobals.cpp`
- `soh/soh/ShipLuaBootstrap.cpp`
- `soh/soh/ShipLuaBootstrap.h`

## Validação executada

Windows 11, Visual Studio 2022 Community, MSVC 19.44, Release x64:

```powershell
cmake -S . -B build-msvc-shiplua -G "Visual Studio 17 2022" -A x64 -DSUPPRESS_WARNINGS=0
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
cmake --build build-msvc-shiplua --config Release --target soh -- /m:1
git diff --check
```

## Resultado da validação

- Configure MSVC passou.
- O build completo passou e gerou `x64/Release/soh.exe` com 40.458.752 bytes.
- O rebuild incremental também passou.
- O diff contém apenas os três arquivos reivindicados e não inclui ativos protegidos.

## Pendências

- O executável não foi iniciado porque o teste local exigiria ativos legítimos não incluídos no repositório.
- Linux e macOS não foram executados localmente.
- Versão e commit do host pertencem a OOT-004.

## Riscos

- O shutdown real com mods carregados ainda depende de execução integrada com ativos legítimos.
- Warnings preexistentes do host e das dependências continuam presentes no MSVC.

## Próxima ação recomendada

Executar OOT-004 em branch própria para fornecer versão e commit do Shipwright ao runtime.

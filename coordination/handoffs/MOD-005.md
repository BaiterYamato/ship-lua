# Handoff — MOD-005 / MOD-006 / MOD-007

## Estado

review

## Resultado

- Dependências obrigatórias são validadas por presença e range SemVer.
- Rejeições propagam apenas para dependentes; mods independentes continuam carregáveis.
- Hints `after`/`before` presentes criam arestas; hints ausentes são ignorados.
- Ordenação topológica usa prioridade crescente e ID como desempate determinístico.
- IDs duplicados, versões inválidas, incompatibilidades e ciclos são reportados por mod.
- A ordem final não depende da ordem de enumeração dos manifestos.

## Commits

- `ed1a0c5` — feat(modloader): resolve dependency graph

## Arquivos alterados

- `include/shiplua/manifest/DependencyResolver.h`
- `src/manifest/DependencyResolver.cpp`
- `tests/unit/DependencyResolverTests.cpp`
- `tests/CMakeLists.txt`
- `coordination/claims/MOD-005.md`
- `coordination/STATUS.md`

## Validação executada

```powershell
cmake --build build-verify
ctest --test-dir build-verify --output-on-failure
git diff --check
rg --files | rg "\.(z64|n64|v64|o2r)$"
```

## Resultado da validação

- Windows 11 / MinGW g++ 15.2 / Ninja: build concluído.
- 6/6 testes CTest passaram.
- `git diff --check` sem erros.
- Nenhum ativo protegido encontrado.

## Pendências

- Conectar a ordem resolvida a uma operação batch do `ModHost` quando o primeiro vertical slice for montado.
- Executar testes em Linux e macOS.

## Riscos

- `ModResolution::rejected` agrega conflitos por ID; duas instâncias duplicadas compartilham um diagnóstico.
- Hints para mods ausentes são intencionalmente ignorados e não constituem dependência.
- Esta branch depende do PR #3 e deve permanecer empilhada até sua integração.

## Próxima ação recomendada

Integrar a pilha em ordem e seguir para TOOL-001 ou API-001/RFC.

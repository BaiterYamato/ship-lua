> English translation of [coordination/handoffs/MOD-001.md](MOD-001.md). The Portuguese document remains the canonical project record.

# Handoff — MOD-001 / MOD-002 / CORE-002

## State

review

## Result

- Manifest represented by `Manifest` and documented by valid JSON Schema.
- TOML parser reads mandatory and optional fields and returns structured errors.
- `ModHost` maintains a `LuaRuntime` isolated by ID, rejects duplicates and rolls back on failure.
- Loading by directory blocks absolute paths, `..` and symlinks in the entrypoint.
- CMake integrates `toml++` and recursively compiles core components.

## Commits

- `61c7675` — docs(governance): allow authorized commits
- `be9d199` — feat(modloader): add manifest host

## Changed files

- `AGENTS.md`
- `CMakeLists.txt`
- `schema/manifest.schema.json`
- `include/shiplua/manifest/Manifest.h`
- `include/shiplua/manifest/ManifestParser.h`
- `src/manifest/ManifestParser.cpp`
- `include/shiplua/host/ModHost.h`
- `src/host/ModHost.cpp`
- `tests/CMakeLists.txt`
- `tests/unit/ManifestHostTests.cpp`
- `coordination/claims/MOD-001.md`
- `coordination/STATUS.md`

## Validation performed

```powershell
cmake -S . -B build -G Ninja -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++
cmake --build build
ctest --test-dir build --output-on-failure
Get-Content schema/manifest.schema.json | ConvertFrom-Json
git diff --check
```

## Validation result

- Windows 11, MinGW g++ 15.2, Ninja: build completed.
- 2/2 CTest tests passed (`runtime_tests` and `manifest_host_tests`).
- JSON Schema parsed successfully.
- `git diff --check` no errors.

## Pending

- Build/test not yet run on Linux and macOS.
- SemVer validation and dependency resolution belongs to MOD-004..MOD-007.
- Valid/invalid extensive fixtures remain in TEST-002.

## Risks

- `FetchContent` still requires network on first configuration.
- The parser ignores unknown keys for forward compatibility.

## Recommended next action

Review and integrate this branch; then start TEST-002/MOD-003.

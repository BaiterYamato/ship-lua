id = "fixture.failing"
name = "Fixture Failing"
version = "0.1.0"
api = ">=0.1 <0.3"
entrypoint = "main.lua"
description = "Fixture do mod test runner: contém uma falha proposital."
authors = ["BaiterYamato"]
games = ["oot", "mm"]

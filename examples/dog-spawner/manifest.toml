id = "community.dog_spawner"
name = "Dog Spawner"
version = "0.2.0"
api = ">=0.2 <0.5"
entrypoint = "main.lua"
description = "Aperte F (rebindável) para spawnar um cachorro. Funciona em OoT (Hyrule Market) e em MM (Clock Town)."
authors = ["BaiterYamato"]
games = ["oot", "mm"]

[capabilities]
optional = ["oot.spawn_dog", "mm.spawn_dog"]

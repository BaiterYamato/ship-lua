id = "baiteryamato.fox_mask"
name = "Fox Mask"
version = "0.1.0"
api = ">=0.4 <0.5"
entrypoint = "main.lua"
description = "Aperte M para vestir a Fox Mask — um item NOVO, com modelo próprio empacotado em fox_mask.o2r, que não existe em nenhum dos dois jogos."
authors = ["BaiterYamato"]
games = ["oot"]

[capabilities]
required = ["oot.player.attach_model", "mod.assets"]
optional = ["player.speed", "player.fields"]

id = "example.actor_spawn"
name = "Generic actor spawn"
version = "0.1.0"
api = ">=0.4 <0.5"
entrypoint = "main.lua"
description = "Exemplo sem IDs nativos usando ship.actor e handles seguros."
authors = ["BaiterYamato"]
games = ["oot", "mm"]

[capabilities]
required = ["actor.spawn", "actor.destroy", "actor.exists"]

[permissions]
grants = ["world.entities.create", "world.entities.destroy", "world.entities.read"]

[limits]
actors = 1

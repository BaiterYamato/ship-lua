id = "baiteryamato.custom_mask"
name = "Máscara customizada (demo)"
version = "0.1.0"
api = ">=0.4 <0.5"
entrypoint = "main.lua"
description = "Demonstra as três primitivas novas: assets de mod, attach_model e player.get/set. Aperte M para vestir um modelo arbitrário na cabeça do Link."
authors = ["BaiterYamato"]
games = ["oot"]

[capabilities]
required = ["oot.player.attach_model"]
optional = ["player.fields", "player.speed"]
